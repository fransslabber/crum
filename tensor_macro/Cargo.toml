[package]
name = "tensor_macro"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "A macro crate for tensor operations"

[dependencies]
syn = { version = "2.0", features = ["full"] }
quote = "1.0"

[lib]
proc-macro = true